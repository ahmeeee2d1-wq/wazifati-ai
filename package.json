{
  "name": "wazifati-ai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build --configLoader native",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check src/app.js && node --check src/cloud.js && node --check src/cv-parser.js && node --check src/matcher.js && node --check src/store.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
